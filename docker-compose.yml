services:
  wuzapi:
    image: asternic/wuzapi:latest
    restart: always
    ports:
      - "8080:8080"
    entrypoint: /bin/sh -c "./wuzapi | tee -a /app/logs/wuzapi.log"
    volumes:
      - ./wuzapi_data:/app/dbdata
      - ./logs/:/app/logs
    environment:
      - WUZAPI_ADMIN_TOKEN=${ADMIN_TOKEN}
      - RABBITMQ_URL=${RABBITMQ_URL}
      - WUZAPI_PORT=8080
      - WUZAPI_GLOBAL_ENCRYPTION_KEY=${WUZAPI_GLOBAL_ENCRYPTION_KEY}
      - WUZAPI_GLOBAL_HMAC_KEY=${WUZAPI_GLOBAL_HMAC_KEY}
      - TZ=Asia/Jerusalem

  rabbitmq:
    image: rabbitmq:3-management
    restart: always
    ports:
      - "5672:5672"   # Messaging port
      - "15672:15672" # Dashboard (optional)
    volumes:
      - ./rabbitmq_data:/var/lib/rabbitmq #save queue in case of container dies
    environment:
      - TZ=Asia/Jerusalem

  logic_engine:
    build: ./code
    container_name: logic_engine  
    volumes:
      - ./code:/app/
      - ./logs:/var/log/logic
    restart: always
    depends_on:
      - rabbitmq
      - wuzapi
    env_file:
      - .env
